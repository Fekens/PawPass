import {useState} from 'react';import {Alert,Pressable,StyleSheet,Text,TextInput} from 'react-native';import {Link,router} from 'expo-router';import {Screen} from '@/components/Screen';import {supabase} from '@/lib/supabase';import {colors,radius} from '@/theme';export default function SignUp(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const submit=async()=>{if(password.length<8)return Alert.alert('Password too short','Use at least 8 characters.');setBusy(true);const{data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{name:name.trim()}}});setBusy(false);if(error)return Alert.alert('Could not create account',error.message);if(!data.session){Alert.alert('Check your email','Confirm your PawPass account, then sign in.');router.replace('/(auth)/sign-in')}else router.replace('/(tabs)')};return <Screen contentStyle={styles.screen}><Text style={styles.eyebrow}>JOIN PAWPASS</Text><Text style={styles.title}>One happy place for every pet.</Text><TextInput style={styles.input} placeholder="Your name" value={name} onChangeText={setName}/><TextInput style={styles.input} autoCapitalize="none" keyboardType="email-address" placeholder="Email address" value={email} onChangeText={setEmail}/><TextInput style={styles.input} secureTextEntry placeholder="Password · 8+ characters" value={password} onChangeText={setPassword}/><Pressable style={styles.button} disabled={busy} onPress={submit}><Text style={styles.buttonText}>{busy?'Creating account…':'Create account'}</Text></Pressable><Text style={styles.footer}>Already have an account? <Link href="/(auth)/sign-in" style={styles.link}>Sign in</Link></Text></Screen>}
const styles=StyleSheet.create({screen:{flexGrow:1,justifyContent:'center'},eyebrow:{fontSize:12,fontWeight:'900',letterSpacing:1.4,color:colors.inkSoft},title:{fontSize:34,fontWeight:'900',color:colors.ink,marginBottom:8},input:{backgroundColor:colors.paper,borderWidth:1,borderColor:colors.line,borderRadius:radius.md,padding:15},button:{backgroundColor:colors.coral,borderRadius:radius.md,alignItems:'center',padding:16},buttonText:{fontWeight:'900',color:colors.ink,fontSize:16},footer:{textAlign:'center',color:colors.muted},link:{color:colors.ink,fontWeight:'800'}});
