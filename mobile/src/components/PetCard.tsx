import {Image,Pressable,StyleSheet,Text,View} from 'react-native';import {router} from 'expo-router';import {Pet} from '@/types';import {colors,radius} from '@/theme';
export function PetCard({pet}:{pet:Pet}){return <Pressable style={({pressed})=>[styles.card,pressed&&{opacity:.92}]} onPress={()=>router.push(`/pet/${pet.id}`)}><View style={styles.photoWrap}>{pet.photo_url?<Image source={{uri:pet.photo_url}} style={[styles.photo,{transform:[{scale:Math.max(1,Number(pet.photo_zoom||1))}]}]} resizeMode="cover"/>:<Text style={styles.emoji}>{pet.animal||'🐾'}</Text>}</View><View style={{flex:1}}><Text style={styles.name}>{pet.name}</Text><Text style={styles.meta}>{pet.breed} · {pet.species}</Text><Text style={styles.status}>{pet.status||'Profile ready'}</Text></View><Text style={styles.chev}>›</Text></Pressable>}
const styles=StyleSheet.create({card:{backgroundColor:colors.paper,borderWidth:1,borderColor:colors.line,borderRadius:radius.lg,padding:14,flexDirection:'row',alignItems:'center',gap:14},photoWrap:{width:72,height:72,borderRadius:18,overflow:'hidden',backgroundColor:colors.mintLight,alignItems:'center',justifyContent:'center'},photo:{width:'100%',height:'100%'},emoji:{fontSize:42},name:{fontSize:19,fontWeight:'800',color:colors.ink},meta:{marginTop:3,color:colors.muted,fontSize:12},status:{marginTop:8,color:colors.inkSoft,fontSize:11,fontWeight:'700'},chev:{fontSize:28,color:colors.inkSoft}});
